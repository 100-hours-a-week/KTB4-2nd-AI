name: AI Worker Image

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/yeodam-ai-worker

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up uv
        uses: astral-sh/setup-uv@c18668ad3cf93ea998bef934396af7bb5c839dc7 # v10.2.0
        with:
          version: "0.12.15"
          enable-cache: true

      - name: Compute model cache key
        id: model
        run: echo "key=$(uv run scripts/fetch_model.py --cache-key)" >> "$GITHUB_OUTPUT"

      - name: Cache model weights
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: models/siglip2
          key: model-${{ steps.model.outputs.key }}

      - name: Fetch model (bf16 vision)
        run: uv run scripts/fetch_model.py
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4.4.1

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        id: build
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: .
          file: docker/Dockerfile
          platforms: linux/amd64
          push: true
          provenance: false
          tags: ${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Record image reference and digest
        run: |
          {
            echo "### AI Worker image"
            echo ""
            echo "\`deploy/image-versions.env\`에 넣을 값:"
            echo ""
            echo '```'
            echo "AI_IMAGE=${IMAGE_NAME}:sha-${GITHUB_SHA}"
            echo "AI_DIGEST=${{ steps.build.outputs.digest }}"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Notify Cloud release manifest
        env:
          GH_TOKEN: ${{ secrets.CLOUD_RELEASE_TOKEN }}
          IMAGE: ${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          jq -n \
            --arg component ai \
            --arg source_repository "${GITHUB_REPOSITORY}" \
            --arg source_sha "${GITHUB_SHA}" \
            --arg image "${IMAGE}" \
            --arg digest "${DIGEST}" \
            '{
              event_type: "image-published",
              client_payload: {
                component: $component,
                source_repository: $source_repository,
                source_sha: $source_sha,
                image: $image,
                digest: $digest
              }
            }' |
            gh api \
              --method POST \
              repos/100-hours-a-week/KTB4-2nd-Cloud/dispatches \
              --input -
